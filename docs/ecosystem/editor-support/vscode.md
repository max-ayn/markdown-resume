# VS Code

*Not implemented yet.*

Planned: syntax highlighting for the marker syntax (`@date`, `@icon`,
`:::block{...}`, ...) and frontmatter schema validation/autocomplete for the
`regions`/`custom.field`/`date` keys documented in the
[YAML reference](/reference/frontmatter-config).
