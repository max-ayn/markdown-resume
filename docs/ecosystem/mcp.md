# MCP

*Not implemented yet.*

Planned: an MCP server exposing `@markdown-resume/core`'s `renderMarkdown`
(and, later, PDF generation) as tools, so an agent can render/validate a
resume without shelling out to the CLI.
