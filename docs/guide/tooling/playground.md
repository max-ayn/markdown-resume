# Playground

*Not implemented yet.*

The plan is an in-browser editor (Markdown + CSS side by side, live-rendered
resume preview) so you can experiment with the marker syntax without
installing anything locally. Until then, use the CLI against the bundled
[examples](https://github.com/max-ayn/markdown-resume/tree/main/examples) —
see [Your first example](/guide/getting-started#your-first-example).
